[build]
builder = "DOCKERFILE"
dockerfilePath = "spec-points-web/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
